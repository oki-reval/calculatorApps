/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Count from './src/scene/App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Count);
